let numero = parseFloat(prompt("Insira um número:")); //A variavel numero irá armazenar o número inserido pelo usuário

if (!isNaN(numero)) 
    if (numero%2 != 0) {
        console.log ("Este número é ímpar:");
        // comando define que número que não é divisivel por 2, é ímpar
    } else

    if (!isNaN(numero)) 
        if (numero%2 == 0) 
            alert ("Este número é par")
            // comando define que o número é par
        
