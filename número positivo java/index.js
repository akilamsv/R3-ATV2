let numero = parseFloat(prompt("Insira um número:")); //A variavel numero irá armazenar o número inserido pelo usuário

if (!isNaN(numero)) {
    if (numero > 0) {
        console.log ("Este número é positivo:");
        // comando define que número maior que 0 positivo
    } else

    if (numero < 0){
        console.log("Este número é negativo:");
        // comando define que número menor que 0 é negativo
    }
}
    
